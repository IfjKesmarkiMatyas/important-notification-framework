{
  "locale": "hu",
  "rules": [
    { "when": "disaster.earthquake.minMagnitude", "level": "critical" },
    { "when": "market.movePercent", "level": "high" },
    { "when": "breaking", "level": "medium" }
  ]
}
